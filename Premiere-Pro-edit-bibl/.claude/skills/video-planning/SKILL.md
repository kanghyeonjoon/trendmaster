---
name: video-planning
description: 리서치 결과를 영상 편집 설계도로 바꾼다. 인트로 훅, 세그먼트 흐름/순서, 페이싱에 맞는 컷 프리셋 추천, 강조/B롤 마커를 타임코드로 기획한다. "영상 기획", "구성 짜줘", "흐름 설계", "인트로 어떻게", "편집 기획안" 요청 시 반드시 사용.
---

# 영상 기획

리서처의 "무엇을 남길지"를 "어떤 순서·속도·강조로 보여줄지"로 옮기는 **편집 설계도**를 만든다.

## 입력
- `output/_workspace/10_research.md`(핵심메시지·하이라이트·삭제추천·챕터)
- `00_director_brief.md`(방향)

## 기획 4종

### 1. 인트로 훅 (첫 5~15초)
리서치 `[인트로후보]` 중 가장 강한 것 선택. 비블 스타일 = **결론/핵심 먼저**. "이걸 깨닫고 10만 넘겼다"처럼 결과를 앞에. 어떤 타임코드 구간을 인트로로 당길지 명시.

### 2. 흐름 / 순서
세그먼트 배열, 끊을 곳·붙일 곳. 리서치 삭제추천을 반영한 최종 구성. 늘어지는 구간 처리(잘라낼지/압축할지).

### 3. 페이싱 → 프리셋 추천
영상 성격으로 컷 공격성 결정:
- 차분한 강의/설명 → 보수
- 일반 토크/라이브 → 표준
- 군더더기 많고 빠른 전개 원함 → 공격
**반드시 프리셋 이름을 명시.** 추가 미세조정은 config 키로 (예: "`HESITATION_MIN`을 0.3으로").

### 4. 강조 / B롤 샷리스트 (타임코드)
편집자가 프리미어에서 작업할 구조화된 메모:
- `[자막강조]` 핵심 문장 — 키워드 볼드/컬러
- `[화면전환]` 주제 바뀌는 지점
- `[B롤]` — 표 형태로: `타임코드 | 무엇을(구체적 비주얼) | 왜(어떤 발화 받쳐주려고) | 길이`
  예: `10:12 | 노출클릭률 대시보드 화면 | "5%" 단언 강조 | 3초`

### 5. 리텐션 / 페이싱 맵
유튜브는 초반 이탈이 생명. 시청 유지를 설계한다:
- **이탈 위험 구간** — 늘어지거나 추상적인 곳(보통 인트로 직후·중반)을 짚고, 패턴 인터럽트(B롤·자막·줌·질문) 배치 제안.
- **페이싱** — `[빠르게]`(군더더기·반복 설명) / `[천천히]`(핵심 결론·감정 고조)를 구간별로 표시. 다 빠르면 피로, 다 느리면 이탈.
- **떡밥 회수** — 인트로에서 던진 호기심(예: "치트키")을 언제 회수하는지 타임코드로.

### 6. 인트로 대본 (첫 20~30초)
콜드 오픈 훅을 실제 문장으로. 구조: ① 결론/충격 한 방 → ② "왜냐하면"의 떡밥 → ③ "끝까지 보면 ~" 약속. 리서치 인트로후보의 발화를 다듬어 쓴다.

## 출력
`output/_workspace/20_plan.md`:
```
## 인트로 훅
00:12:30~00:12:48 "..." 를 0초로 당김
## 흐름
1. 훅 → 2. 본론A(05:30~) → ...
## 프리셋 추천
표준 (이유: ...). 미세조정: HESITATION_MIN=0.3
## 강조/B롤 마커
- 00:08:15 [자막강조] "구체성이 핵심"
- 00:15:40 [B롤] 썸네일 예시 화면
```

## 왜 이렇게 하는가
- 컷편집가가 프리셋 명을 그대로 실행에 넣을 수 있어야 파이프라인이 안 끊긴다 → 추상적 조언 금지, 실행 가능한 지시만.
- 강조/B롤은 자동화 못 하는 사람의 영역 → 비블이 바로 작업하도록 타임코드로 넘긴다.
